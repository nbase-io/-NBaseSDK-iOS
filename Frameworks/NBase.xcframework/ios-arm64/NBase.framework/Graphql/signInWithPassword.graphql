mutation signInWithPassword($projectId: String!, $storeId: String!, $username: String!, $password: String!, $options: String) {
  signInWithPassword(input: {projectId: $projectId, storeId: $storeId, username: $username, password: $password, options: $options}) {
    member {
      id
    }
    token
  }
}
